import { ButtonVariant } from "../types/buttons";

export const buttonsConfig: Record<ButtonVariant, string> = {
    primary: "bg-primary text-surface",
    outline: "border border-surface-container-highest",
}